spring:
  application:
    name: system-server # 应用名
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848   #nacos服务注册发现
      config:
        server-addr: localhost:8848  #nacos配置中心
        file-extension: yml #配置中心的配置文件格式
  profiles:
    active: dev  #默认启动开发环境    system-server-dev.yml